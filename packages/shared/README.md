# Shared API contract

Generated TypeScript types for Smart Market Watchlist's versioned HTTP API. Both the
web and mobile clients import this private workspace package; application code must not
edit `api.generated.ts` directly.

Regenerate it from the backend with `make api-types` at the repository root.
